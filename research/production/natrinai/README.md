# Naṟṟiṇai R2 production

Active phase: R2 under schema `0.4.0`.

Frozen source: `corpus/natrinai/poems/`. Never modify canonical poems for research classification.

The adapter keeps canonical poem body, YAML heading metadata and printed Markdown source note provenance-distinct. A printed context note may support role/relationship observations but is never flattened into poem-body evidence.

Production complete: `001–400` form a gap-free ledger with **6,007 observations**, exact 29-dimension decisions per record and no next-record pointer. Publication history preserves benchmark `001–002`, stabilization `003–010`, regular 25-record batches, and final `386–400`. Every poem is reviewed sequentially/source-first across the exact 29 dimensions. Tolkāppiyam and conventional tiṇai associations never auto-populate poem dimensions.

Preservation controls: record 234 has no canonical body and retains candidate quotations only as source-note apparatus; record 385 remains incomplete with lacuna markers unchanged.
